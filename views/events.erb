<div class="wrapper">
  <%= partial :side_nav_bar %>
  <div class="content">
    <div id="events-container">Events</div>
    <template id="events-item">
      <div class="title">{{ title }}</div>
      <div class="date">{{ day }} {{ date }} {{ month }} {{ year }}</div>
      <div class="report">{{{ event-details }}}</div>
    </template>
  </div>
</div>